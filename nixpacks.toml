providers = ["node"]

[phases.setup]
nixPkgs = ["bun", "nodejs_22"]

[phases.install]
cmds = ["bun install --frozen-lockfile || bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run preview"
